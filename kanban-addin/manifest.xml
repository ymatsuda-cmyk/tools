<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<OfficeApp xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
           xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
           xsi:type="TaskPaneApp">

  <!-- ============ Basic Settings ============ -->
  <!-- Replace with your own generated GUID -->
  <Id>460d2c5c-e4cd-4d96-82f4-703849c080a0</Id>

  <Version>1.0.0.0</Version>
  <ProviderName>ymatsuda</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>

  <DisplayName DefaultValue="WBS Kanban" />
  <Description DefaultValue="Display WBS sheet as Kanban board and sync changes back to WBS." />

  <SupportUrl DefaultValue="https://ymatsuda-cmyk.github.io/tools/kanban-addin/" />

  <!-- Domains that can be opened in Dialog/TaskPane/Command (GitHub Pages) -->
  <AppDomains>
    <AppDomain>https://ymatsuda-cmyk.github.io</AppDomain>
  </AppDomains>

  <Hosts>
    <Host Name="Workbook" />
  </Hosts>

  <!-- ReadWriteDocument required for Excel sheet read/write -->
  <Permissions>ReadWriteDocument</Permissions>

  <!-- Backward compatibility: minimum SourceLocation required -->
  <DefaultSettings>
    <SourceLocation DefaultValue="https://ymatsuda-cmyk.github.io/tools/kanban-addin/blank.html" />
  </DefaultSettings>

  <!-- ============ Add-in Commands (Ribbon) ============ -->
  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
                    xsi:type="VersionOverridesV1_0">

    <Hosts>
      <Host xsi:type="Workbook">
        <DesktopFormFactor>

          <!-- FunctionFile where JS for ExecuteFunction is located -->
          <FunctionFile resid="Commands.Url" />

          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <CustomTab id="WbsKanbanTab">
              <Label resid="Tab.Label" />

              <Group id="WbsKanbanGroup">
                <Label resid="Group.Label" />

                <!-- Ribbon button: calls openKanban (ExecuteFunction) on click -->
                <Control xsi:type="Button" id="OpenKanbanButton">
                  <Label resid="Btn.OpenKanban.Label" />
                  <Supertip>
                    <Title resid="Btn.OpenKanban.Title" />
                    <Description resid="Btn.OpenKanban.Desc" />
                  </Supertip>
                  <Action xsi:type="ExecuteFunction">
                    <FunctionName>openKanban</FunctionName>
                  </Action>
                </Control>

              </Group>
            </CustomTab>
          </ExtensionPoint>

        </DesktopFormFactor>
      </Host>
    </Hosts>

    <Resources>
      <bt:Urls>
        <!-- FunctionFile (commands.html): associates openKanban -->
        <bt:Url id="Commands.Url" DefaultValue="https://ymatsuda-cmyk.github.io/tools/kanban-addin/commands.html" />
      </bt:Urls>

      <bt:ShortStrings>
        <bt:String id="Tab.Label" DefaultValue="WBS Kanban" />
        <bt:String id="Group.Label" DefaultValue="Operations" />
        <bt:String id="Btn.OpenKanban.Label" DefaultValue="Open Kanban" />
        <bt:String id="Btn.OpenKanban.Title" DefaultValue="WBS Kanban" />
      </bt:ShortStrings>

      <bt:LongStrings>
        <bt:String id="Btn.OpenKanban.Desc"
                   DefaultValue="Load WBS sheet content and display as Kanban board in dialog. Changes are synced back to WBS sheet." />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>

</OfficeApp>